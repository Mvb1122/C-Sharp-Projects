// As this isn't a part of the existing "Learning C#" series, I'm not creating a REPL to run it.
    // Plus, this program is simple enough that you don't need help to figure out what it does.

// BTW, I just made this to just to test a setup of .NET C# on my laptop, which runs Linux.
using System;

namespace _demo {
    internal class Program {
        public static void Main(string[] args) {
            Console.WriteLine("Works!");
        }
    }
}